# ============================================================
# CERAVIS edge — runtime deps for NATIVE Jetson deploy (system Python 3.10).
#
# JetPack ALREADY provides — never pip install these (pip wheels would
# shadow the JetPack builds and break GStreamer/CUDA):
#   numpy      (apt: python3-numpy)
#   opencv     (apt: python3-opencv — GStreamer-enabled)
#   tensorrt   (JetPack SDK component + python3-libnvinfer)
#   CUDA       (JetPack)
#
# Installed separately by scripts/install_native.sh:
#   pycuda       (pip, compiles against /usr/local/cuda)
#   supervision  (pip --no-deps, so it can't drag in opencv-python)
#
# NOT runtime deps at all:
#   torch / torchvision / ultralytics — used once for ONNX export inside a
#   disposable venv (scripts/export_engines.sh). Inference is pure TensorRT.
#
# Freeze after a working install:  pip3 freeze --user > requirements.lock
# ============================================================

# ---- Web / API ----
fastapi
uvicorn[standard]
pydantic
pydantic-settings
websockets

# ---- Events / alerts / similarity ----
paho-mqtt
faiss-cpu

# ---- needed by supervision (installed --no-deps) ----
pyyaml
defusedxml
pillow
scipy
matplotlib
