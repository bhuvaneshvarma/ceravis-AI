# ============================================================
# CERAVIS edge — Python dependencies for the Jetson (aarch64, JetPack 6).
#
# JetPack ALREADY provides these — do NOT pip install them:
#   numpy      (apt: python3-numpy)
#   opencv     (apt: python3-opencv, built with GStreamer + CUDA)
#   tensorrt   (JetPack)
#
# torch / torchvision are installed separately in Dockerfile.jetson from the
# jetson-ai-lab CUDA index (the PyPI build has no Jetson GPU support), so keep
# them OUT of this file.
#
# Versions are intentionally unpinned for the first build. After a successful
# build, freeze with:  docker exec ceravis-edge pip3 freeze > requirements.lock
# ============================================================

# ---- Web / API ----
fastapi
uvicorn[standard]
pydantic
pydantic-settings
websockets

# ---- AI / inference ----
ultralytics
supervision
faiss-cpu
paho-mqtt
pycuda
